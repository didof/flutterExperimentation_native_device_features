import 'package:flutter/material.dart';

class BodyPlacesList extends StatelessWidget {
  const BodyPlacesList({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: CircularProgressIndicator(),
    );
  }
}
